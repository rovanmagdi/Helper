import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const StyledBoxFooter = styled(Box)(({ theme }) => ({
    fontWeight: "900", fontFamily:['Comfortaa', "cursive"],fontSize:"1.5rem"
}));
